# Woshmart — Build Log

Updated on every phase merge, not before. This is the single source of truth for "what's actually done" — `docs/BUILD_SCRIPT.md` checkboxes track task-level progress *within* a phase; this table tracks phase-level completion and links the evidence.

**Rule:** a phase is only marked Complete here once its PR has merged with CI green and its exit criteria (per `BUILD_SCRIPT.md`) have been met and verified — not when the code is written, not when CI is pending.

## Phase tracker

| Phase | Status | Branch | PR | CI | Started | Completed | Notes |
|---|---|---|---|---|---|---|---|
| 0 — Pre-work & scaffolding | Complete | `phase-0-scaffolding` | [#1](https://github.com/zortojnr/woshmart-project/pull/1) | green | 2026-07-20 | 2026-07-20 | |
| 1 — Foundation | Complete | `phase-1-foundation` | [#2](https://github.com/zortojnr/woshmart-project/pull/2) | green | 2026-07-21 | 2026-07-21 | |
| 2 — Conversation engine core | Complete | `phase-2-conversation-engine-core` | [#4](https://github.com/zortojnr/woshmart-project/pull/4) | green | 2026-07-21 | 2026-07-21 | |
| 3 — Full conversation flow | Not started | — | — | — | — | — | |
| 4 — Woshmen, partners, keyword protocol | Not started | — | — | — | — | — | |
| 5 — Admin API + Retool | Complete | `phase-5-admin-api-retool` | [#9](https://github.com/zortojnr/woshmart-project/pull/9) | green | 2026-07-22 | 2026-07-23 | Retool build + live exit-criteria walkthrough performed and reported by the human tester, not independently observable by Claude Code — see notes below |
| 6 — Timeouts, sweeps, resilience | Complete | `phase-6-timeouts-sweeps-resilience` | [#17](https://github.com/zortojnr/woshmart-project/pull/17) | green | 2026-07-23 | 2026-07-23 | This row was left stale at "Not started" after the actual merge — backfilled here while updating for Phase 7, see notes below |
| 7 — Security & production hardening | Complete | `phase-7-security-hardening` | [#20](https://github.com/zortojnr/woshmart-project/pull/20) | green | 2026-07-23 | 2026-07-24 | |
| 8 — Launch readiness | In progress | `phase-8-launch-readiness` | [#22](https://github.com/zortojnr/woshmart-project/pull/22) (merged) | green | 2026-07-24 | — | **Still not marked Complete — see rule above, and updated 2026-07-28.** Production infra is now genuinely live and confirmed healthy (Web Service, Supabase database, Render Key Value Redis — `/health` returns `{"ok":true,"db":"up","redis":"up"}`, see `docs/PHASE_8_PRODUCTION_CUTOVER.md` §§1–3), a real, concrete milestone. But `BUILD_SCRIPT.md`'s actual exit criteria is "first real customer order completes successfully end-to-end on production," and a healthy `/health` check isn't that — no production order has been placed yet. Still required before this row moves to Complete: Twilio webhooks pointed at production, `super_admin` seeded in production, pricing sign-off, monitoring review, and a first real production order completing end-to-end. Partner-side `READY` success path also still untested live — tracked in the Post-MVP log below, needed before Phase 8a regardless. |
| 8a — Supervised pilot | Not started | — | — | — | — | — | Not a code phase — log pilot order outcomes here, not a PR |

**Status values:** `Not started` / `In progress` / `Blocked` / `In review` / `Complete`

If a phase is `Blocked`, add a line in Notes explaining what it's blocked on (e.g. "Blocked — WhatsApp Business Profile still in Meta review") — don't leave a blocked phase silently sitting as "In progress."

## Per-phase completion record

Copy this template into a new section below for each phase as it completes — this is the detail behind the summary table above.

```
### Phase N — <name>

- **Merged:** <date>, PR #<number> (<link>)
- **CI:** green on merge — <link to the passing run>
- **Exit criteria met:** <restate the exit criteria from BUILD_SCRIPT.md and confirm each was verified, with evidence — e.g. "tampered webhook request confirmed rejected with 403, see test <name>">
- **What was built:** <short summary>
- **What was tested:** <unit/integration/manual, and what manual verification was actually run>
- **Deferred / explicitly not done:** <anything intentionally left out, and why — should match anything flagged in the PR description>
- **Issues found during the phase:** <anything that came up — a PRD ambiguity that had to be resolved, a decision that deviated from the docs and why>
```

## Per-phase completion record

### Phase 0 — Pre-work & scaffolding

- **Merged:** 2026-07-20, PR #1 (https://github.com/zortojnr/woshmart-project/pull/1)
- **CI:** green on merge — run 29745154859
- **Exit criteria met:** Phase 0's code-facing exit criterion is repo scaffolding existing, matching `ARCHITECTURE.md` §4, with CI passing typecheck/lint/test — confirmed. (The ops-facing exit criterion — a WhatsApp message to the sandbox number producing a raw payload in a local log — depends on Phase 1's webhook handler and the human/ops setup in `docs/SETUP_GUIDE.md`; not applicable to this code-only PR.)
- **What was built:** Node.js + TypeScript (strict) project per `TRD.md` §1's stack; full `ARCHITECTURE.md` §4 folder structure with real stub files; `package.json` scripts (dev/build/test/lint/format/typecheck); ESLint + Prettier; GitHub Actions CI (typecheck + lint + test); `.env.example` per `SETUP_GUIDE.md` §4; `.gitignore`.
- **What was tested:** `npm run typecheck`, `npm run build`, `npm run lint`, `npm test` all run clean locally and in CI (placeholder smoke test only — real tests start Phase 1).
- **Deferred / explicitly not done:** all business/conversation logic, env validation logic, Prisma migration, webhook handling — everything Phase 1+.
- **Issues found during the phase:** `ARCHITECTURE.md` §4 shows the Prisma schema nested at `src/db/prisma/schema.prisma`; the real, populated schema (matching `DATABASE_SCHEMA.md`) already lived at the conventional root `prisma/schema.prisma`. Kept the root file as source of truth rather than duplicating it — flagged in the PR. Also: the repo had no commits on `main` at all before this phase, so the first scaffolding commit was pushed directly to `main` to give the PR a base.

### Phase 1 — Foundation

- **Merged:** 2026-07-21, PR #2 (https://github.com/zortojnr/woshmart-project/pull/2)
- **CI:** green on merge — run 29812067871
- **Exit criteria met:** a genuinely-signed webhook request (computed with the real Twilio algorithm, not a mock) is accepted; a tampered request and a garbage/missing signature are provably rejected with 403 (`tests/webhooks/twilio.signature.test.ts`, both cases). A replayed `MessageSid` produces exactly one `messages` row on both the inbound and status-callback paths (`tests/webhooks/idempotency.test.ts`). `/health` correctly reports DB/Redis up/down. Env validation fails fast and loudly on missing/malformed required vars. CI green with typecheck + lint + 18 tests, against ephemeral Postgres/Redis service containers.
- **What was built:** `src/config/env.ts` (Zod-validated env loading, fails fast on boot); Prisma schema + first migration applied to the dev DB, plus a second migration adding Postgres `CHECK` constraints for enum-like columns; `src/webhooks/twilio.validate.ts` (signature validation via the official `twilio.validateRequest` helper, reconstructing the public URL from forwarded proto/host headers); `src/webhooks/twilio.controller.ts` (`POST /webhooks/twilio/inbound` and `/status`, both idempotent by `MessageSid`); `GET /health`; Pino structured logging with request-id correlation.
- **What was tested:** automated — signature validation (valid/tampered/garbage/missing), idempotency (inbound + status), env validation (valid boot, missing var, malformed `DATABASE_URL`, short `JWT_SIGNING_SECRET`, partial object-storage config), health checks (DB down, Redis down). Manual — signed/tampered/replayed requests run against a live dev server with the real dev Twilio Auth Token before automating them; `/health` manually confirmed against real dev Postgres/Redis.
- **Deferred / explicitly not done:** all business/conversation logic, FSM, keyword parsing — everything Phase 2+.
- **Issues found during the phase:** `SENTRY_DSN` is listed in `SETUP_GUIDE.md` §4 as required before Phase 1, but Sentry isn't wired into any code until Phase 7 — made optional in `env.ts` rather than blocking boot on an unused external service, flagged in the PR for Phase 7 to revisit. `DATABASE_SCHEMA.md`/`TRD.md` §6's per-column `CHECK` constraints had no first-class Prisma representation in this version — added as a second raw-SQL migration instead of editing the already-applied first one.

### Phase 2 — Conversation engine core

- **Merged:** 2026-07-21, PR #4 (https://github.com/zortojnr/woshmart-project/pull/4)
- **CI:** green on merge — run 29833991837
- **Exit criteria met:** two-turn conversation works end-to-end on the real sandbox — verified live against a real phone and the real Twilio WhatsApp sandbox (via a Cloudflare quick tunnel to a local dev server), with every turn cross-checked directly against the `messages`/`sessions` tables (not just the phone screen): welcome copy exact, out-of-coverage/waitlist copy exact (Kpakungu), in-zone bundle-menu copy exact (Maitumbi), session state persisting correctly turn to turn (`WELCOME` → `COVERAGE_CHECK` → `SERVICE_SELECTION`). Malformed/unrecognized area input falls through to the out-of-coverage response without crashing (unit-tested and confirmed by design, not separately re-run live). Also automated end-to-end in `tests/webhooks/conversation.e2e.test.ts` against the real webhook/signature/DB/engine path.
- **What was built:** `src/conversation/types.ts` (FSM types per `TRD.md` §2); `src/conversation/session.repository.ts` (load-or-create by `phone_number`, atomic upsert); `src/conversation/engine.ts` (pure orchestration, no business logic inline); `WELCOME`/`COVERAGE_CHECK` state handlers as pure functions; `src/domain/zones/zone.service.ts` (coverage keyword-matching per `PRD.md` §7); `src/messaging/twilio.client.ts` + `send.service.ts` (the only code path that calls Twilio's send API, retry/backoff on transient failures, no retry on permanent ones, every send logged to `messages`); webhook controller now dispatches non-duplicate inbound messages to the engine.
- **What was tested:** unit tests for both state handlers as pure `(context, input) → result` functions, covering in-zone/waitlist/not-yet-available/unrecognized input and both branches of the waitlist YES/NO follow-up; `send.service` unit tests (success, transient retry-then-succeed, permanent no-retry, retry-budget exhaustion) against a mocked Twilio client with fake timers; a real end-to-end webhook integration test; a live manual sandbox test (see PR #4 description for the full transcript with real Twilio SIDs).
- **Deferred / explicitly not done:** everything past `COVERAGE_CHECK` (Phase 3); Woshman/partner keyword routing (Phase 4 — every inbound message currently goes to the customer FSM); the `MARK_WAITLISTED` side effect is logged by the engine but not yet wired to a real domain service (no waitlist/user-flagging service exists until a later phase touches `User` data).
- **Issues found during the phase:** `PRD.md` §10 has no specified copy for the bot's reply after a customer answers YES/NO to the waitlist offer (`TRD.md` §3 only specifies the state transition, not the message) — flagged rather than inventing new copy; a YES currently ends the session silently after logging the side effect. Worth a copy decision before Phase 8a's pilot.

### Phase 5 — Admin API + Retool

- **Merged:** 2026-07-22, PR #9 (https://github.com/zortojnr/woshmart-project/pull/9)
- **CI:** green on merge — build run 29923910931
- **Exit criteria met:** yes — see the split below between what Claude Code independently verified (code, tests, CI, direct API calls) versus what the human tester performed and reported in Retool/WhatsApp, which Claude Code has no access to and cannot independently re-check.
  - ✅ **Verified by Claude Code directly:** a `viewer`-role token is provably (tested, not assumed) rejected on every write route — `tests/admin-api/rbac.test.ts` asserts, per route, a missing-token 401, a wrong-role 403, and the correct role's 200 on the identical request. Manually re-read `auth.middleware.ts`/`rbac.middleware.ts` line by line per this phase's human-scrutiny requirement, not just trusted the tests. Audit logging verified with a real `admin_actions` row assertion (before/after values, not just "a row exists"). Order status/assign endpoints confirmed rejecting illegal transitions the same way the keyword parser does. Staging deploy itself — Render Web Service + separate Postgres + separate Key Value instance stood up per `docs/PHASE_5_RETOOL_WALKTHROUGH.md`, migrations applied, `super_admin` and a `viewer` test account seeded (`scripts/seed-super-admin.ts`'s `[role]` arg, PR #15), login round-trips confirmed for both against the live staging URL.
  - ✅ **Reported by the human tester (2026-07-23), not independently observable by Claude Code:** the Retool app itself was built (resource, login flow, Orders/Users/Woshmen/Partners/Pricing/Feedback/Messages screens) per `docs/PHASE_5_RETOOL_WALKTHROUGH.md` Part B; RBAC role-gating re-verified live in the Retool UI with the real seeded `viewer` account across every write control; the Assign action was exercised end to end and produced a real WhatsApp dispatch notification. Claude Code has no Retool or WhatsApp access, so this line records the tester's direct account of their own testing, not a claim Claude Code confirmed itself.
- **What was built:** `POST /admin/auth/login` (bcryptjs + JWT, ~8hr expiry); `auth.middleware.ts` + `rbac.middleware.ts` wired on every route from the first one; `audit.middleware.ts`'s `recordAudit()` called synchronously by every write controller as its last step before responding (deterministic, not fire-and-forget) plus `auditGuardMiddleware` as a safety net that logs loudly if any write route ever skips it; every `docs/TRD.md` §5.2 route (orders list/filter/detail/status/assign, users, woshmen, partners, pricing, feedback, manual message send); `scripts/seed-super-admin.ts` (including the later `[role]` argument, PR #15, for creating `viewer`/`ops` test accounts); `scripts/seed-test-order.ts` and `scripts/seed-test-woshman-partner.ts` for populating Retool test data without a full WhatsApp conversation; `docs/PHASE_5_RETOOL_WALKTHROUGH.md` for the Render + Retool setup steps; the Retool app itself (COO screens, login flow, role-gated UI), built by the human tester following that walkthrough.
- **What was tested:** auth (valid/wrong-password/unknown-email/inactive-admin/missing/malformed/expired/forged-secret tokens); RBAC (401/403/200 triple on all seven write routes, including the `super_admin`-only pricing route rejecting both `viewer` and `ops`); audit logging (a successful write produces one correct `admin_actions` row with real before/after values, a rejected write produces none); order status/assign parity with the existing statemachine's illegal-transition rejection. `npm run typecheck`/`lint`/full test suite green in CI against an ephemeral local Postgres container. Separately, the full Retool walkthrough (verify a transfer, mark PAID, assign Woshman+partner, confirm WhatsApp notification, viewer blocked from every write) performed live by the human tester against staging.
- **Deferred / explicitly not done:** `pricing_config` write is storage/API only, `computeQuote()` still reads the static `bundles.config.ts` (wiring live pricing into quotes is Phase 9+ per `docs/DATABASE_SCHEMA.md`); `partners.outstandingBalanceKobo`/`partners.lastRating` have no write path anywhere in the Admin API (logged as a Phase 9+ item in `docs/BUILD_SCRIPT.md`, PR #14).
- **Issues found during the phase:** `PRD.md` §12's notification matrix lists the Woshman "dispatch brief" / Partner "job brief" under the PAID row, but no Woshman/partner is actually known until the separate, later ASSIGNED action (`docs/USER_JOURNEY.md` explicitly says "on assignment") — confirmed with the user that the briefs should fire from `assign`, not `status→paid`. Also surfaced and fixed a real bug: `JSON.stringify` can't serialize the `BigInt` money columns Prisma returns, so every order/user/woshman/partner JSON response was 500ing — fixed with a `BigInt.prototype.toJSON` shim (`src/lib/bigintJson.ts`). Separately, the staging deploy's first attempt pointed `DATABASE_URL` at the dev Neon database instead of the new Render staging Postgres instance — caught via a "table admins does not exist" error at seed time, corrected in the Web Service's environment variables.

### Phase 6 — Timeouts, sweeps, resilience

- **Merged:** 2026-07-23, PR #17 (https://github.com/zortojnr/woshmart-project/pull/17)
- **CI:** green on merge — build run 30043851743
- **Exit criteria met:** kill/restart resilience verified live on staging by the human tester — reached `PAYMENT_METHOD` at 20:39, Render "Restart Service" triggered, replied at 20:53 (14-minute gap), session correctly resumed to the exact right quote rather than resetting to WELCOME, confirming both session state and the still-scheduled timeout job survived the restart. Not independently observable by Claude Code (no Render or WhatsApp access) — recorded here as the tester's own account.
- **What was built:** BullMQ job infrastructure (`src/jobs/queue.ts`, dependency-free shared job-naming/timing constants in `jobIds.ts`); 30-min quote-abandon timeout, 60-min payment-window-abandon timeout with a 45-min reminder, 24hr auto-close (`sessionTimeout.job.ts`, `autoClose.job.ts`); dead-letter handling (`logJobFailure`, logged loudly on exhausted retries, not retried indefinitely or silently dropped); `order.statemachine.ts` wired to schedule/cancel jobs directly on every status transition, not duplicated per call site.
- **What was tested:** idempotency for every new job handler (run twice, assert no duplicate side effects) plus a real admin-API idempotency gap found and fixed (`PATCH /admin/orders/:id/assign` was re-sending the dispatch notification on a same-Woshman/partner retry — fixed with an `assignmentChanged` guard, regression test proves both the no-op-retry and genuine-reassignment cases). `npm run typecheck`/`lint`/full test suite green in CI. Manual: a local proxy-based kill/restart check, superseded by the real staging verification above.
- **Deferred / explicitly not done:** rate limiting, Sentry, backups, and alert-threshold configuration — all explicitly scoped to Phase 7, not started here.
- **Issues found during the phase:** two real bugs caught before merge — BullMQ silently rejects any custom `jobId` containing a colon ("Custom Id cannot contain :"), so every `scheduleJob`/`cancelJob` call was failing silently until `jobIds.ts`'s separator was changed from `:` to `-`; and ioredis's default indefinite reconnect retry meant a Redis outage would never actually trip the `try/catch` around scheduling, fixed with a bounded `connectTimeout` plus a `withTimeout()` race. Also: this row itself was left at "Not started" in the phase tracker above after the actual merge — an example of exactly the gap CLAUDE.md's "update on every merge" rule exists to prevent — backfilled here on 2026-07-24 while updating the log for Phase 7.

### Phase 7 — Security & production hardening

- **Merged:** 2026-07-24, PR #20 (https://github.com/zortojnr/woshmart-project/pull/20)
- **CI:** green on merge — build run 30093115180 (merge commit `d14810a`)
- **Exit criteria met:** audit-and-fix pass completed item by item (full mapping was the PR description) — rate limiting active and tested (per-phone/global webhook, per-admin/IP Admin API, outbound throttle); every Admin API route confirmed already Zod-validated; money confirmed integer-kobo everywhere via a full grep sweep; git history (65 commits) swept for secrets, none found; PII-in-logs review found and fixed one real gap (`quote.ts` logging the customer's raw address at error level); error tracking wired end-to-end against a mocked Sentry module (real-DSN verification still pending, tracked below); alert thresholds configured per `CLAUDE.md`'s philosophy, with an explicit test proving a single transient failure does not page. One item explicitly not completed in-session: production Twilio signature validation against the exact prod URL — needs real production Twilio console access this session doesn't have.
- **What was built:** `src/lib/rateLimiter.ts` (Redis fixed-window counter) plus webhook and Admin API rate-limit middleware; an outbound send throttle in `send.service.ts`; `src/lib/sentry.ts` + a super_admin-only `POST /admin/diagnostics/test-error` route for verification; `src/lib/alertEmail.ts` (a narrowly-scoped urgent email, fired only when a payment/data-integrity-critical job dead-letters); a PII fix in `quote.ts`. A significant follow-on scope, once it was discovered mid-phase that `woshmart-staging-db` is on Render's free tier with a 44-day hard-deletion clock, not just missing backups: `.github/workflows/staging-backup.yml` (daily `pg_dump` → Backblaze B2, 7+ day retention via a B2 lifecycle rule) and `scripts/check-staging-db-deadline.ts` (an escalating reminder email starting well before the deadline, self-updating via the database's own earliest `_prisma_migrations` timestamp rather than a hardcoded date that someone would have to remember to update after a restore).
- **What was tested:** new tests for the rate limiter core (against real Redis), both rate-limit middleware layers, the outbound throttle's spacing behavior, the Sentry-capture wiring, the dead-letter alert-email trigger (and, just as importantly, that it does *not* fire while retries remain), and the deadline-reminder script's edge cases (verified empirically first against real scratch Postgres databases — a brand-new never-migrated database and an empty-but-existing migrations table both fail loudly with a clear, secret-free error rather than crashing or silently computing a wrong date). Full local suite and CI both green on the final merge commit.
- **Deferred / explicitly not done:** production Twilio signature verification (needs real prod console access); actually performing a Render backup/restore test (documented in `docs/SECURITY.md` §3.9, needs Render dashboard access); enabling Render's native alert notifications (documented, needs dashboard access); real end-to-end Sentry capture against a provisioned `SENTRY_DSN`; a data-retention policy decision (flagged as open, not resolved).
- **Issues found during the phase:** three real secret-exposure near-misses happened during this phase's work (Redis password, staging DB password, local dev DB password — all logged in `docs/SECURITY.md` §4.4, all rotated), which surfaced a real gap in how debugging commands were being run rather than a code defect — closed by adding a new non-negotiable rule to `CLAUDE.md` (never print a secret's raw value in tool output/logs/terminal, even for debugging) rather than treating it as a one-off lesson. Separately, a real merge conflict emerged after this branch was cut: `main` picked up a fully-pinned `package.json`/`package-lock.json` (a parallel, unrelated dependency-pinning PR) while this branch added new dependencies using the old `^`-prefixed style — resolved by merging `main` in and re-pinning the new additions to match, with the lockfile diff verified line-by-line before pushing.

### Phase 8 — Launch readiness (IN PROGRESS — not Complete, see summary table above)

- **PR #22 merged:** 2026-07-25 (https://github.com/zortojnr/woshmart-project/pull/22), CI green. This covers the *verification and documentation* scope of the phase's prompt in `BUILD_SCRIPT.md` — it is not the same thing as the phase being done, and is not being treated as such here.
- **Exit criteria — NOT yet met:** `BUILD_SCRIPT.md`'s Phase 8 exit criteria is "first real customer order completes successfully end-to-end on production with no manual workaround required outside the defined COO steps." No production order has been placed; production cutover (`docs/PHASE_8_PRODUCTION_CUTOVER.md`) has not been executed, per CLAUDE.md's rule that go-live is a deliberate human action, not something a docs PR performs.
- **What was actually done in PR #22:** a full live staging E2E walkthrough against a real order (`WM-003`, `docs/PHASE_8_STAGING_E2E_WALKTHROUGH.md`) — order placement through WhatsApp, COO actions in Retool, the full Woshman/partner keyword-driven status progression, the feedback flow (score-3 escalation path specifically), and deliberately-wrong keyword rejections (malformed command, wrong-sender `READY`, nonexistent order, repeat-command idempotency) — each cross-checked directly against `order_status_history`/`messages` rather than taken on WhatsApp delivery timing alone.
- **Real bugs found and handled during this pass:**
  - Retool's Mark PAID button was sending uppercase `"PAID"` against the Admin API's case-sensitive status enum — a Retool-config bug, not a backend bug (the backend correctly rejected it per CLAUDE.md rule 4). Fixed in Retool.
  - `assigned → pickup_scheduled` has no automated trigger anywhere in the code — confirmed as a genuine missing-automation bug (not intentional), logged in the Post-MVP table below as a follow-up fix, not addressed in this docs-only PR.
  - `messages.ts` copy had drifted from an already-revised `docs/PRD.md` §10 (em dashes → simpler punctuation), and the Woshman dispatch brief was printing a raw stored pickup-window id instead of readable text — both fixed in a separate PR (#24), not this one.
- **Deferred / explicitly not done (as of this PR):** production cutover in full — all prepared/documented per `docs/PHASE_8_PRODUCTION_CUTOVER.md` and `docs/PHASE_8_LAUNCH_READINESS_SUMMARY.md` but not yet executed; WhatsApp template approval still blocked on Meta business verification; the live partner-side `READY` success path (no second test number available — tracked in the Post-MVP log below).
- **Update, 2026-07-28 — production infra now live:** worked through `docs/PHASE_8_PRODUCTION_CUTOVER.md` §§1–3 live. Real deviations from the original plan, decided deliberately along the way, not silently:
  - **Database is Supabase (free tier), not Render Postgres.** Render Postgres's paid-tier requirement was the original plan; switched after weighing the actual tradeoffs — Supabase's free tier pauses rather than permanently deletes on inactivity, is one-click restorable, and needs no card. `DATABASE_URL` uses Supabase's Session Pooler connection string specifically (Direct is IPv6-only and Transaction Pooler isn't safe for `pg_dump`-style persistent-session workloads).
  - **Web Service is free tier, not paid** — same live-tradeoff reasoning, spin-down risk to be mitigated by a keep-alive ping (UptimeRobot, planned — not yet confirmed set up, see Post-MVP log).
  - **Region is Frankfurt for both Web Service and database**, deliberately diverging from staging's Oregon — backend↔database latency compounds across every sequential query in a request, which dominates over the backend↔customer hop (largely governed by Twilio's own network path regardless of region).
  - **A real production build failure was hit and fixed along the way:** `npm ci` was silently skipping `devDependencies` (including `typescript` itself) under `NODE_ENV=production`, causing a `Cannot find module '.../dist/server.js'` crash loop on the very first deploy. Root-caused empirically (not guessed), fixed by changing the Build Command to `npm ci --include=dev && npm run build` — see the Post-MVP-adjacent PR #35 and `docs/PHASE_8_PRODUCTION_CUTOVER.md` §3 for the full investigation. Staging and CI confirmed unaffected (`NODE_ENV` is `"staging"`/`"test"` respectively, neither triggers npm's production-mode skip).
  - **A real, still-open gap surfaced by this pass:** production has no backup/disaster-recovery plan of its own (unlike staging's `pg_dump` → B2 pipeline). Logged as a new Post-MVP item below — needs addressing before real customer/payment data accumulates.
- **Next step to close this phase out:** point production Twilio webhooks at the live service, seed production `super_admin`, get pricing sign-off, complete a first real production order end-to-end, then move this row to Complete with that as the evidence — not before.

## Post-MVP (Phase 9+) log

Track ad hoc — these aren't sequential/gated the way Phases 0–8 are, so log each as it's picked up rather than pre-listing rows.

| Item | Status | PR | Notes |
|---|---|---|---|
| Staging DB free-tier backup + deletion-deadline reminder | Done | (Phase 7 follow-up) | `woshmart-staging-db` is on Render's free tier — created **2026-07-24**, hard deletion at **2026-09-06** (44 days) unless upgraded first. Daily `pg_dump` → Backblaze B2 (7+ day retention via B2 lifecycle rule) and an escalating email reminder from day 28 (self-updating — reads the DB's own earliest migration timestamp, no hardcoded date) both run via `.github/workflows/staging-backup.yml`. Full procedure and dates in `docs/SECURITY.md` §3.9. |
| **Bug:** `assigned → pickup_scheduled` has no automated trigger | Not started | (Phase 8 finding) | Found during Phase 8 verification, then confirmed as a genuine missing-automation bug (not an intentional manual COO step) by cross-checking `USER_JOURNEY.md`'s sequence diagram, which goes straight from assignment to the Woshman's `COLLECTED` message with no gap — see `docs/PHASE_8_LAUNCH_READINESS_SUMMARY.md`. **Fix:** wire an automatic `assigned → pickup_scheduled` transition into `assignOrder()` (`src/domain/orders/order.service.ts`), mirroring the existing `paid → assigned` auto-transition already in that same function. Also found in the same pass: `PICKUP_SCHEDULED` and `PAID` are both missing from `NotificationEvent` (`src/domain/notifications/notification.service.ts`) — neither currently notifies the customer when it fires, contradicting `USER_JOURNEY.md`'s diagram (`Bot->>C: Payment confirmed` right after `Mark PAID`). Fix both in the same follow-up PR, kept separate from Phase 8's docs-only PR and from the WhatsApp template fallback PR (#23). |
| Partner-side `READY` success path not yet tested live | Not started | (Phase 8 finding, PR #22) | Found during Phase 8's staging E2E walkthrough (`docs/PHASE_8_STAGING_E2E_WALKTHROUGH.md`, `WM-003`) — no second WhatsApp number was available to register as a real test partner. What *was* confirmed live: a Woshman-number sender correctly gets rejected from `READY` (sender-authorization check), and the resulting transition + Woshman-alert notification are covered by `tests/webhooks/keywordProtocol.e2e.test.ts` in isolation. What's still unverified is the real thing — a genuine partner number sending `READY`, the order transitioning to `ready_for_delivery`, and the Woshman actually receiving the alert, all live against staging. Doesn't block merging PR #22 (Phase 8 is docs/verification-only). **Should be tested before Phase 8a's supervised pilot** — real partners will use this path in production, and it's the one keyword-protocol success path Phase 8 didn't confirm live. |
| `woshmart-staging-redis` still on Render's free tier | Not started | — | Confirmed via dashboard 2026-07-25. No forced deletion clock (unlike Postgres) since free Key Value instances only lose data on restart/maintenance, not a calendar deadline, but worth upgrading before Phase 8a's supervised pilot to avoid stuck-order cleanup friction if a restart wipes in-flight BullMQ timeout jobs during a live-watched test. |
| **Bug:** Woshman `active` field is dead code for keyword-routing | Not started | — | Found while trying to take a test Woshman out of the keyword-routing rotation for a customer-flow test without deleting the record. `findWoshmanByPhone` (`src/domain/woshmen/woshman.service.ts`) does an unconditional `prisma.woshman.findUnique({ where: { phoneNumber } })` with no `active` filter, and `src/webhooks/twilio.controller.ts` routes purely on whether that lookup returns non-null. So setting `active: false` via `updateWoshman` has zero effect on routing — a deactivated Woshman's messages still go to the keyword parser, not the customer flow, unlike `admin.active`, which genuinely gates login. **Fix:** add an active check to `findWoshmanByPhone` (or wherever routing decides keyword-parser vs. customer-flow). Not fixed here, outside scope of today's session, but worth closing before it matters in real operations — e.g. a Woshman leaving and needing to be taken out of rotation without deleting their record/history. Workaround in the meantime: `scripts/swap-woshman-phone.ts` temporarily changes the phone number instead. |
| Customer session never refreshes/expires based on order-status progress | Not started | — | Found while investigating a stale session stuck at `AWAITING_PAYMENT` for an order (`WM-002`) that had actually progressed normally to `at_laundry` days earlier through the real keyword sequence — the order was fine, only the session's cached context was stale. Traced the cause: nothing in the codebase updates a customer's `sessions` row in response to order-status progress, except one deliberate exception — `keywordProtocol.service.ts`'s `DELIVERED` handler explicitly calls `saveSession(order.user.phoneNumber, 'FEEDBACK_PENDING', ...)`. Every other keyword-driven transition (`paid → assigned → pickup_scheduled → picked_up → at_laundry → ready_for_delivery → out_for_delivery`) touches only `orders`/`order_status_history`, never the session. So a customer messaging the bot anytime between `paid` and `out_for_delivery` hits `awaitingPaymentHandler`'s deliberate silence (`src/conversation/states/payment.ts`) regardless of how far the order has actually progressed, since the session is still frozen at `AWAITING_PAYMENT`. Real customer-experience impact, not just test-data cleanliness: a customer checking in mid-order today would get total silence, which looks broken even though the system is technically working as designed for the narrow "awaiting payment verification" case it was built for. **Fix:** either refresh the session's context/state as the linked order progresses, or give `awaitingPaymentHandler` (or a new handler covering the post-payment span) an actual reply — at minimum a status check or "your order is being processed" acknowledgment — instead of silence for the entire remaining order lifecycle. Not fixed here, outside scope of today's session. |
| Production Supabase database has no backup/disaster-recovery plan | Not started | — | Found 2026-07-28 while updating docs for production going live. Staging has a documented, working `pg_dump` → Backblaze B2 pipeline (`docs/SECURITY.md` §3.9) because its Render Postgres free tier has a hard 44-day deletion clock; production's Supabase free tier doesn't have that specific clock (it pauses, not deletes, and is one-click restorable), but "no clock" isn't the same as "has a backup plan" — there is currently **zero** backup/export mechanism for production's actual customer and payment records. **Needs addressing before real customer traffic accumulates meaningfully**, not after — this is the one database in the whole system where data loss would be genuinely severe, given there's no payment gateway to reconcile against. |
| Production keep-alive ping (UptimeRobot) — planned, not yet confirmed set up | Not started | — | Decided during the free-tier Web Service tradeoff discussion (2026-07-28): a 5-minute-interval UptimeRobot monitor against production `/health`, comfortably under Render's 15-minute free-tier spin-down threshold. GitHub Actions cron was considered and rejected for this specific purpose — its scheduling jitter (documented 5–30+ minute delays under load) can exceed the 15-minute window it needs to beat. Not yet confirmed actually created — worth verifying before relying on it to keep the service warm for real customer traffic. |
| Confirmed: staging is NOT at risk from the `NODE_ENV=production`/`npm ci` devDependency bug | Done | (verification only) | Directly tested 2026-07-28, not assumed: `NODE_ENV=staging npm ci` in an isolated clean directory installed the full 289-package set with `typescript` present, matching the no-`NODE_ENV` control exactly. npm's devDependency-skip behavior is specifically keyed to the literal string `"production"`; staging's `NODE_ENV` is `"staging"`, so it was never exposed to the bug documented in `docs/PHASE_8_PRODUCTION_CUTOVER.md` §3 / PR #35. No fix needed on staging's Build Command. |
